<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.GiphyService" parent="Theme.Material3.DayNight.NoActionBar"/>

    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/window_splash_screen_background</item>
        <item name="postSplashScreenTheme">@style/Base.Theme.GiphyService</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/animated_logo</item>
    </style>

</resources>